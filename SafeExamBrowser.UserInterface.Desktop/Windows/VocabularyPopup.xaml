<Window x:Class="SafeExamBrowser.UserInterface.Desktop.Windows.VocabularyPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Height="40" Width="400" WindowStartupLocation="Manual">
    <Border Background="White" CornerRadius="8" BorderBrush="#007ACC" BorderThickness="2">
        <Grid Margin="10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputBox" Grid.Column="0" 
                     Background="Transparent" Foreground="Black" 
                     BorderThickness="0" FontSize="16" 
                     VerticalContentAlignment="Center"
                     CaretBrush="Black" />
            <TextBlock x:Name="ResultText" Grid.Column="1" 
                       Foreground="#007ACC" FontSize="16" 
                       VerticalAlignment="Center" Margin="10,0,0,0"
                       FontWeight="SemiBold" />
        </Grid>
    </Border>
</Window>

